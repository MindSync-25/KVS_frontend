import 'package:flutter/material.dart';

class ReferralDashboardPage extends StatelessWidget {
  const ReferralDashboardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ReferralDashboardPage')),
      body: const Center(child: Text('ReferralDashboardPage — coming soon')),
    );
  }
}
